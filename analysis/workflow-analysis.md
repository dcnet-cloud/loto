# DCNET Lô Tô - Workflow Hệ Thống

## Tổng Quan

Ứng dụng gọi số Lô Tô với hệ thống câu rao 4 tầng random, đảm bảo mỗi lượt chơi hoàn toàn khác biệt và không thể đoán trước.

## Kiến Trúc File

```
loto/
├── index.html       ← Giao diện (sidebar + grid)
├── styles.css       ← CSS responsive (mobile-first)
├── data.js          ← Câu rao văn hóa/bài hát (số 1-15)
├── app.js           ← Logic game + sinh câu toán động
├── build.js         ← Build production (minify + obfuscate)
├── assets/
│   └── logo.jpg     ← Logo DCNET Telecom
└── analysis/        ← Tài liệu phân tích
```

## Luồng Chính

```
┌─────────────┐     ┌─────────────┐
│  GỌI SỐ     │     │  TỰ ĐỘNG    │
│  (manual)    │     │  (auto)     │
└──────┬──────┘     └──────┬──────┘
       │                   │
       └───────┬───────────┘
               ▼
  ┌─────────────────────────┐
  │  ① RANDOM SỐ            │
  │  remaining[] → pick 1   │
  │  (crypto.getRandomValues)│
  └────────────┬────────────┘
               ▼
  ┌─────────────────────────┐
  │  Update UI               │
  │  • Hiện số lên display   │
  │  • Highlight ô trên grid │
  │  • Cập nhật "Đã gọi"    │
  └────────────┬────────────┘
               ▼
  ┌─────────────────────────┐
  │  ② RANDOM POOL (33% mỗi)│
  │  [culture, math, null]  │
  └────────────┬────────────┘
               │
       ┌───────┼───────────┐
       ▼       ▼           ▼
  ┌────────┐ ┌────────┐ ┌────────┐
  │ 🎵 VĂN │ │ 🔢 TOÁN│ │ ⬜ NULL│
  │  HÓA   │ │  HỌC   │ │       │
  └───┬────┘ └───┬────┘ └───┬───┘
      │          │           │
      ▼          ▼           │
 ┌─────────┐ ┌──────────┐   │
 │③ Random  │ │③ Random  │   │
 │câu trong │ │loại phép │   │
 │lottoData │ │tính      │   │
 │[number]  │ └────┬─────┘   │
 └────┬─────┘      │         │
      │            ▼         │
      │  ┌──────────────┐    │
      │  │④ Random số   │    │
      │  │trong phép    │    │
      │  │tính đó       │    │
      │  └──────┬───────┘    │
      │         │            │
      │    ┌────┴────────────┤
      │    │                 │
      ▼    ▼                 ▼
  ┌────────────┐      ┌───────────┐
  │ Có câu rao │      │ Không có  │
  │            │      │ câu rao   │
  └─────┬──────┘      └─────┬─────┘
        │                   │
        ▼                   ▼
  ┌───────────────┐   ┌───────────────┐
  │ Đọc câu rao   │   │ Đọc "Số X"   │
  │ (TTS vi-VN)   │   │ (TTS vi-VN)   │
  └───────┬───────┘   └───────┬───────┘
          │                   │
          ▼                   │
  ┌───────────────┐           │
  │ ⏳ Chờ 3 giây  │           │
  │ (user đoán)    │           │
  └───────┬───────┘           │
          ▼                   │
  ┌───────────────┐           │
  │ Đọc "là con   │           │
  │ số X"          │           │
  └───────┬───────┘           │
          │                   │
          └─────────┬─────────┘
                    ▼
           ┌──────────────┐
           │ Còn số không?│
           └──────┬───────┘
              ┌───┴───┐
              ▼       ▼
           ┌────┐  ┌──────┐
           │ CÓ │  │ HẾT  │
           └──┬─┘  └──┬───┘
              │       │
         ┌────┴──┐    ▼
         │Auto?  │  Game Over
         └───┬───┘  "HẾT"
          ┌──┴──┐
          ▼     ▼
       ┌─────┐ ┌────────┐
       │ YES │ │  NO    │
       └──┬──┘ └───┬────┘
          │        │
          ▼        ▼
    ┌──────────┐  Chờ user
    │Chờ X giây│  bấm tiếp
    │(interval)│
    └────┬─────┘
         │
         └──→ Quay lại ① RANDOM SỐ
```

## Chi Tiết: Hệ Thống 4 Tầng Random

### Tầng 1: Random Số

- Dùng `crypto.getRandomValues()` (cryptographic-secure)
- Pick 1 số từ mảng `remaining[]`
- Số đã gọi không bao giờ lặp lại trong cùng ván

### Tầng 2: Random Pool (Category)

Pool **cố định 3 loại** cho **TẤT CẢ** số 1-60:

| Category | Xác suất | Mô tả |
|----------|----------|-------|
| `culture` | 33% | Câu rao văn hóa/bài hát |
| `math` | 33% | Câu rao toán học (sinh động) |
| `null` | 33% | Đọc "Số X" bình thường |

**Lưu ý:** Nếu trúng `culture` nhưng số đó chưa có câu rao văn hóa → fallback đọc số bình thường.

### Tầng 3: Random Trong Category

#### Nếu trúng Văn hóa (`culture`):
- Random chọn 1 câu trong `lottoData[number]`
- Hiện tại số 1-15 đã có câu rao, số 16-60 sẽ bổ sung dần

#### Nếu trúng Toán học (`math`):
- Xác định các phép tính **khả dụng** cho số đó:

| Phép tính | Điều kiện | Áp dụng cho |
|-----------|-----------|-------------|
| Cộng (`add`) | n ≥ 2 | Hầu hết số |
| Trừ (`sub`) | Luôn có | Tất cả |
| Nhân (`mul`) | Có ước > 1 | Số hợp |
| Chia (`div`) | Luôn có | Tất cả |
| Căn bậc hai (`sqrt`) | Luôn có | Tất cả |
| Bình phương (`square`) | n là số chính phương | 4, 9, 16, 25, 36, 49 |
| Lũy thừa 3 (`cube`) | n là lập phương hoàn hảo | 8, 27 |
| Lũy thừa 2 (`pow2`) | n là lũy thừa của 2 | 2, 4, 8, 16, 32 |

- Random chọn 1 loại phép tính từ danh sách khả dụng

### Tầng 4: Random Số Trong Phép Tính

Mỗi loại phép tính sinh số ngẫu nhiên khác nhau:

| Phép tính | Cách sinh | Ví dụ (n=56) |
|-----------|-----------|------------|
| **Cộng** | a = rand(1, n-1), b = n-a | "Hai mươi ba cộng ba mươi ba bằng mấy?" |
| **Trừ** | b = rand(1, 30), a = n+b | "Bảy mươi tám trừ hai mươi hai bằng mấy?" |
| **Nhân** | Pick cặp ước (i, n/i) | "Bảy nhân tám bằng mấy?" |
| **Chia** | b = rand(2, 10), a = n×b | "Hai trăm tám mươi chia năm bằng mấy?" |
| **Căn bậc hai** | Tính n² | "Căn bậc hai của ba ngàn một trăm ba mươi sáu bằng mấy?" |
| **Bình phương** | Tính √n | "Bảy bình phương bằng mấy?" (n=49) |
| **Lũy thừa 3** | Tính ∛n | "Ba mũ ba bằng mấy?" (n=27) |
| **Lũy thừa 2** | Tính log₂(n) | "Hai mũ năm bằng mấy?" (n=32) |

## Chi Tiết: Luồng Sinh Câu Toán

```
generateMathPhrase(n)
       │
       ▼
┌──────────────────────────────────┐
│ Xác định các phép tính khả dụng │
│ cho số n:                        │
│                                  │
│ ✅ Luôn có: sub, div, sqrt       │
│ ✅ n ≥ 2:  + add                 │
│ ✅ Có ước:  + mul                 │
│ ✅ n = k²:  + square             │
│    (4,9,16,25,36,49)             │
│ ✅ n = k³:  + cube               │
│    (8, 27)                       │
│ ✅ n = 2ᵏ:  + pow2               │
│    (2,4,8,16,32)                 │
└──────────────┬───────────────────┘
               ▼
┌──────────────────────────────────┐
│ ③ RANDOM loại phép tính          │
└──────────────┬───────────────────┘
               │
  ┌────┬───┬───┼────┬─────┬─────┬──────┐
  ▼    ▼   ▼   ▼    ▼     ▼     ▼      ▼
┌───┐┌───┐┌──┐┌──┐┌────┐┌────┐┌────┐┌─────┐
│ + ││ - ││ ×││ ÷││ √  ││ x² ││ x³ ││ 2ᵏ  │
└─┬─┘└─┬─┘└┬─┘└┬─┘└──┬─┘└──┬─┘└──┬─┘└──┬──┘
  │    │   │   │     │     │     │     │
  ▼    ▼   ▼   ▼     ▼     ▼     ▼     ▼
┌──────────────────────────────────────────┐
│ ④ RANDOM các số trong phép tính           │
│                                           │
│  +  : a = rand(1,n-1), b = n-a            │
│  -  : b = rand(1,30), a = n+b             │
│  ×  : pick random cặp ước (i, n/i)        │
│  ÷  : b = rand(2,10), a = n×b             │
│  √  : tính n²                             │
│  x² : tính √n (chỉ số chính phương)       │
│  x³ : tính ∛n (chỉ 8→2, 27→3)            │
│  2ᵏ : tính log₂(n) (chỉ lũy thừa 2)     │
└──────────────────────┬────────────────────┘
                       ▼
          numberToVietnamese(số)
          → chuyển thành tiếng Việt
          → "Bảy nhân tám bằng mấy?"
```

## Luồng Speech (TTS)

```
speak(number)
     │
     ├─── Có câu rao ──────────────────┐
     │                                  │
     │    sayText(câu rao)              │
     │         │                        │
     │         ▼                        │
     │    ⏳ delay(3000ms)              │
     │    (user đoán đáp án)            │
     │         │                        │
     │         ▼                        │
     │    sayText("là con số X")        │
     │                                  │
     ├─── Không có câu rao ────────┐    │
     │                             │    │
     │    sayText("Số X")          │    │
     │                             │    │
     └─────────────────────────────┘    │
                                        │
     Bất kỳ lúc nào: speakAbort=true    │
     → hủy speech ngay (khi reset)      │
```

## Chế Độ Tự Động (Auto Mode)

```
startAuto()
     │
     ▼
 ┌──────────┐
 │ autoTick │◄────────────────┐
 └────┬─────┘                 │
      │                       │
      ▼                       │
  doCall()                    │
  (gọi số + speech)          │
      │                       │
      ├── OK ────→ setTimeout(autoTick, X giây)
      │
      └── HẾT ──→ stopAuto() → gameOver()

  X = intervalInput.value (default 5s, range 1-30s)
```

## Hàm numberToVietnamese

Chuyển đổi số thành tiếng Việt chuẩn cho TTS:

| Số | Tiếng Việt | Ghi chú |
|----|-----------|---------|
| 0 | không | |
| 1 | một | |
| 5 | năm | |
| 10 | mười | Không phải "một mươi" |
| 11 | mười một | Dùng "một" (không phải "mốt") |
| 15 | mười lăm | Dùng "lăm" (không phải "năm") |
| 21 | hai mươi mốt | Dùng "mốt" (không phải "một") |
| 25 | hai mươi lăm | Dùng "lăm" |
| 100 | một trăm | |
| 102 | một trăm lẻ hai | "lẻ" cho hàng đơn vị |
| 105 | một trăm lẻ năm | "năm" (không phải "lăm" vì không có hàng chục) |
| 115 | một trăm mười lăm | "lăm" vì có hàng chục |
| 1000 | một ngàn | |
| 3600 | ba ngàn sáu trăm | Max cho căn bậc hai (60²) |

## Dữ Liệu Câu Rao Văn Hóa

Hiện tại **số 1-15** có câu rao văn hóa trong `data.js`. Số 16-60 sẽ bổ sung dần.

| Số | Chủ đề câu rao |
|----|---------------|
| 1 | Bắc kim thang (đồng dao) |
| 2 | Dậy đi em (bài hát) |
| 3 | Tình cha (bài hát) |
| 4 | Gặp nhau bên gốc chuối (bài hát) |
| 5 | Em đi trên cỏ non (bài hát) |
| 6 | Không phải tại anh (bài hát) |
| 7 | Chị ong nâu (đồng dao) |
| 8 | Bài tình ca đất phương Nam (bài hát) |
| 9 | Từ ngày hai đứa yêu nhau (bài hát) |
| 10 | Ba thương con (đồng dao) |
| 11 | Í nhột í nhột |
| 12 | Trúc Anh Đài giả trai |
| 13 | Hồng trần (bài hát) |
| 14 | Tề thiên đại thánh (truyện) |
| 15 | Cô Thắm về làng (bài hát) |

**Cách thêm câu rao mới:** Thêm vào `lottoData` trong `data.js`:
```javascript
const lottoData = {
  // Thêm nhiều câu cho số đã có:
  1: [
    'Bắc kim thang cà lang bí rợ...',
    'Câu rao mới cho số 1',
  ],
  // Thêm số mới:
  16: [
    'Câu rao cho số 16',
    'Câu rao thứ 2 cho số 16',
  ],
};
```

## Responsive Layout

```
Mobile Portrait (< 768px):
┌──────────────────┐
│     Header       │
│     Display      │
│    Controls      │
│  Auto Settings   │
│                  │
│   Number Grid    │
│   (9 x 7)       │
│                  │
└──────────────────┘

Desktop (≥ 768px):
┌────────┬─────────────────┐
│ Header │                 │
│Display │   Number Grid   │
│Controls│    (9 x 7)      │
│  Auto  │                 │
└────────┴─────────────────┘

Mobile Landscape:
┌──────┬───────────────────┐
│Displ.│                   │
│Ctrls │   Number Grid     │
│ Auto │    (9 x 7)        │
└──────┴───────────────────┘
```

## Tech Stack

- **Frontend:** Vanilla HTML/CSS/JS (không framework)
- **TTS:** Web Speech API (`speechSynthesis`, giọng vi-VN)
- **Random:** `crypto.getRandomValues()` (cryptographic-secure)
- **Layout:** CSS Flexbox + Grid, `display: contents` trick
- **Build:** Node.js + Terser + javascript-obfuscator + clean-css
- **Deploy:** Single file `dist/index.html` (inline all)
